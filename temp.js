function emotions(myString, myFunc) {
    console.log("I am " + myString + ", " + myFunc(2));
}

// your code goes here


emotions("happy",Func(num) {
  var sound = ""  
  for(var i = 1;i <= num;i++) {
  sound = sound +"ha"}
  sound = sound + "!"
  return sound });
